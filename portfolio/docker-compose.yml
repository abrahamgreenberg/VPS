services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:${PORTFOLIO_PORT}:80"
    restart: unless-stopped
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
