version: '3.8'
services:
  test:
    build: .
    container_name: test
    restart: always
    ports:
      - "4001:4001"
    volumes:
      - ./nodejs:/app/nodejs
      - ./python:/app/python
      - ./cronfile:/etc/cron.d/mycron
    environment:
      - NODE_ENV=production
