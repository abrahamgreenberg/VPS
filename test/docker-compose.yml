version: '3.8'
services:
  test:
    build: .
    container_name: test
    restart: always
    ports:
      - "4001:4001"
    volumes:
      - ./nodejs:/app/nodejs   # Node.js code
      - ./python:/app/python   # Python scripts
    environment:
      - NODE_ENV=production
